# Web Design Pro Stone

**ID:** `stone-web-design-pro`  
**Status:** `CANDIDATE`  
**Evidence target:** repository-local `TEST`

Web Design Pro turns canonical resume content into a distinctive, responsive, accessible, and inspectable web surface. It is subordinate to canonical facts and PSYSOC-X dignity boundaries.

## Core Contract

```text
canonical content + PSYSOC-X profile + artifact manifest
    -> information architecture
    -> semantic HTML + design tokens + resilient CSS
    -> responsive/accessibility/link verification
    -> preview receipt
    -> production promotion gate
```

## Resource Surface

- `personas/personas.json` — Web Design Pro, Accessibility Auditor, and Release Verifier.
- `skills/SKILLS.md` — information architecture, tokens, responsive layout, semantics, accessibility, SEO, and release verification.
- `resources/design-system.json` — design tokens and zero-script budgets.
- `tools/tools.json` — bounded design and verification tools.
- `connectors/connectors.json` — source, deployment, browser, artifact, DNS, and header connectors.
- `templates/` — actual semantic HTML and responsive CSS candidate.
- `tests/cases.json` — first-screen signal, script-free, semantic, responsive, accessibility, and artifact-link checks.

## Non-Claims

A template is not a deployed website. A limited accessibility audit is not certification. A preview is not production. Visual prominence cannot promote evidence.
